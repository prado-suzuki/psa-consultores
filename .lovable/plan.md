

# Renomear aba "Usuários" → "Usuários Estrutura"

## Alteração em `src/pages/equipe/EquipeControleAcessos.tsx`

Trocar o texto do `TabsTrigger` com `value="usuarios"` de "Usuários" para "Usuários Estrutura". Apenas mudança de label, sem alterar values internos.

