// Só estilo e dado, sem componente: as três peças do checklist do cliente e o pai
// compartilham o anel de foco, e o pai não pode importar token de estilo de um
// filho. Mesmo papel de `equipe/osg/onboarding/onboardingKit.ts`.

/** Anel de foco dos botões nativos do checklist do cliente. */
export const FOCO = 'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40';
